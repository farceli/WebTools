# Cloudflare Pages 重定向规则
# 支持 React Router 前端路由

# 所有页面都重定向到 index.html（SPA支持）
/*    /index.html   200

# API路由（如果将来需要）
/api/*    /api/:splat   200

# 静态资源保持原样
/assets/*    /assets/:splat   200
/icons/*     /icons/:splat    200
